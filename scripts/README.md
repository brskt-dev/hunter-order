# Scripts

Repository automation must be deterministic, non-interactive by default and safe to run locally. Scripts that mutate data must require an explicit flag and must never target production automatically.
