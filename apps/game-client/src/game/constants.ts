// Phaser-free game constants. Kept in their own module so they can be unit
// tested without importing the Phaser runtime.

/** Base design resolution. The canvas scales to fit the viewport from here. */
export const GAME_WIDTH = 1280;
export const GAME_HEIGHT = 720;

/** Solid background color for the boot screen. */
export const BACKGROUND_COLOR = '#0d0f14';
