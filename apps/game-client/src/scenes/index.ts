import { BootScene } from './boot-scene';

export { BootScene } from './boot-scene';

/**
 * Ordered list of scene classes registered with Phaser. The first entry is the
 * scene Phaser starts automatically. Add new scenes here as they are created.
 */
export const sceneClasses = [BootScene];
